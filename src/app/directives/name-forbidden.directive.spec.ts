import { NameForbiddenDirective } from './name-forbidden.directive';

describe('NameForbiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new NameForbiddenDirective();
    expect(directive).toBeTruthy();
  });
});
